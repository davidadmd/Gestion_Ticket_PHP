{% extends "layout/base.twig" %}

{% block title %}Accès refusé - {{ parent() }}{% endblock %}

{% block content %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-body text-center">
                    <h1 class="display-1 text-danger mb-4">403</h1>
                    <h2 class="mb-4">Accès refusé</h2>
                    <p class="lead mb-4">{{ message|default('Vous n\'avez pas les permissions nécessaires pour accéder à cette page.') }}</p>
                    <a href="{{ path('/') }}" class="btn btn-primary">
                        <i class="fas fa-home me-2"></i>Retour à l'accueil
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
